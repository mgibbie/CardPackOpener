{
  "name": "cardpackopener",
  "private": true,
  "type": "module",
  "description": "Static site + Cloudflare Pages Functions backend (D1) for /login accounts",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "puppeteer-core": "^25.3.0"
  },
  "scripts": {
    "deploy-art": "wrangler pages deploy battlecards/art --project-name magepunk-cardart --branch main --commit-dirty=true"
  }
}
