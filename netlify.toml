[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# clean API path for the account backend
[[redirects]]
  from = "/api/mp"
  to = "/.netlify/functions/mp"
  status = 200
